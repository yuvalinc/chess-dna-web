{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); case \"$cmd\" in *--no-verify*|*--force*|*'push --force'*|*'reset --hard'*|*'rm -rf'*) echo '🚫 Blocked destructive command' >&2; exit 2;; esac; exit 0"
          }
        ]
      }
    ]
  }
}