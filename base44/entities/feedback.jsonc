{
  "name": "Feedback",
  "description": "Free-form user feedback captured by the floating FeedbackButton: bug reports, feature requests, and beta impressions. Reviewed by admins through the /feedbacks page.",
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "email": { "type": "string" },
    "mobile": { "type": "string" },
    "category": { "type": "string" },
    "message": { "type": "string" },
    "rating": { "type": "number" },
    "page": { "type": "string" },
    "userAgent": { "type": "string" },
    "submittedAt": { "type": "number" }
  },
  "required": ["message"],
  "rls": {
    "create": true,
    "read": {
      "user_condition": { "role": "admin" }
    },
    "update": {
      "user_condition": { "role": "admin" }
    },
    "delete": {
      "user_condition": { "role": "admin" }
    }
  }
}
